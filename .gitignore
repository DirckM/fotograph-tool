# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage
/test-results
.superpowers/

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
logs/

# training dataset (6.2GB of client photos — lives on the RunPod volume, not git)
/Training Images/

# Claude Code per-session worktrees.
.claude/worktrees/

# Experiment inputs. Source footage that goes INTO a workflow is not something
# this repo produces, and a client's marketing video has no business in git.
# Each experiment's README records what its input was and where it sits.
experiments/*/input/
